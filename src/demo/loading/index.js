
define( [ "ui/loading/loading-ng" ], function() {

    "use strict";

    angular
    .module( "demo.loading", [ "$ui.loading" ] )
    .controller( "loadingController", [ "$scope", function( $scope ) {

    } ] );
} );
